;;; secrets --- Variables that should not be in the tracked repo

;;; Commentary:
;;; Copy this skeleton file and setup the variables

;;; Code:

(setq calendar-latitude ...)
(setq calendar-longitude ...)
(setq calendar-time-zone ...)

(provide 'secrets)

;;; secrets.el ends here